# Nixpacks configuration for Node.js 20
[variables]
NODE_ENV = "production"

[phases.setup]
nixPkgs = ["nodejs_20", "npm-10_x"]

[phases.install]
cmds = ["npm ci --omit=dev"]

[start]
cmd = "npm start"